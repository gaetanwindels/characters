/**
 * Configuration file for the server.
 */
var spacename = require('./spacename');

spacename.hi();

spacename.CHARACTERS.config = {
	/* Color of the background */
	COLOR_BACKGROUND: "green",
	/* Size of the tiles, in px */
	TILE_SIZE: 20,
	/* */
};
